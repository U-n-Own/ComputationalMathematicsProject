\documentclass{article}

\usepackage[margin = 3cm]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{framed}
\usepackage{sidenotes}
\usepackage{longtable}
\usepackage{makecell}
\usepackage{biblatex}
\usepackage{graphicx}
\usepackage{float}
\usepackage{algorithm}
\addbibresource{main.bib}

\lstdefinelanguage{Julia}%
  {morekeywords={abstract,break,case,catch,const,continue,do,else,elseif,%
      end,export,false,for,function,immutable,import,importall,if,in,%
      macro,module,otherwise,quote,return,switch,true,try,type,typealias,%
      using,while},%
   sensitive=true,%
   alsoother={\$},%
   morecomment=[l]\#,%
   morecomment=[n]{\#=}{=\#},%
   morestring=[s]{"}{"},%
   morestring=[m]{'}{'},%
}[keywords,comments,strings]%

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    language = Julia,
    backgroundcolor=\color{white},
    commentstyle=\itshape,
    keywordstyle=\bfseries,
    numberstyle=\tiny\color{codegray},
    stringstyle=\itshape,
    basicstyle=\small,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2,
    mathescape=true
}

\lstdefinestyle{mystyle_tt}{
    language = Julia,
    frame=single,
    backgroundcolor=\color{backcolour},
    commentstyle=\itshape,
    keywordstyle=\bfseries\color{codepurple},
    numberstyle=\tiny\color{codegray},
    stringstyle=\itshape,
    basicstyle=\small\ttfamily,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2,
    mathescape=true
}

\lstdefinestyle{mystyle_plain}{
    frame=single,
    backgroundcolor=\color{white},
    commentstyle=\itshape,
    keywordstyle=\small\ttfamily,
    numberstyle=\tiny\color{codegray},
    stringstyle=\itshape,
    basicstyle=\small\ttfamily,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2,
    mathescape=false
}

\lstset{style=mystyle}

\newcommand{\reals}{\mathbb R}
\newcommand{\rmatr}[2]{\reals ^ {{#1} \times {#2}} }
\newcommand{\rvect}[1]{\reals ^ {{#1}} }

\newcommand{\spann}{\text{span }}
\newcommand{\hess}{\underbar H}
\newcommand{\arnoldi}{\texttt{Arnoldi}}

\newtheorem{remark}{Remark}
\newtheorem{claim}{Claim}
\newtheorem{theo}{Theorem}

\title{Computational Mathematics for Learning and Data Science Report: Analyzing an iterative approach to solve for Ill-Conditioned Linear Systems}
\author{Vincenzo Gargano, Lorenzo Pace}
\begin{document}
%  \section*{Notation List}
% 
% \begin{longtable}{r|r}
%   $D$ & \ A diagonal matrix \dotfill \pageref{symbol:D} \\
%   $E$ & \ \makecell{A matrix obtained by removing the last row from \\the node-arc incidence matrix of a given connected directed graph }\dotfill \pageref{symbol:E} \\
%   $x$ & \ A variable we want to solve for \dotfill \pageref{symbol:x} \\
%   $y$ & \ Vector of the linear system \dotfill \pageref{symbol:y} \\
%   $b$ & \ Vector of the linear system \dotfill \pageref{symbol:b} \\
%   $c$ & \ Vector of the linear system \dotfill \pageref{symbol:c} \\
%   $A$ & \ Matrix of the linear system \dotfill \pageref{symbol:A} \\
%   $H_n$ & \ The $n \times n$ upper Hessenberg matrix \dotfill \pageref{symbol:H_n} \\
%   \hess & \ The $n+1 \times n$ Hessenberg matrix \dotfill \pageref{symbol:hess} \\
%   $K_n$ & \ The Krylov space of dimension $n$ \dotfill \pageref{symbol:K_n} \\
%   $Q_n$ & \ The matrix of the Arnoldi basis \dotfill \pageref{symbol:Q_n} \\
%   $\alpha$ & \ The diagonal of the Hessenberg matrix \dotfill \pageref{symbol:alpha} \\
%   $\beta$ & \ The subdiagonal of the Hessenberg matrix \dotfill \pageref{symbol:beta} \\
%   $Q'$ & \ The matrix $Q$ with an extra row and column \dotfill \pageref{symbol:Q'} \\
%   $H_{refl}$ & \ The Householder reflection matrix \dotfill \pageref{symbol:H_{refl}} \\
%   $O$ & \ The orthogonal matrix that makes $Q'^T_{old} \hess_n$ triangular \dotfill \pageref{symbol:O} \\
%   $R$ & \ The upper triangular matrix of the QR factorization of $\hess_n$ \dotfill \pageref{symbol:R} \\
%   $Q$ & \ The orthogonal matrix of the QR factorization of $\hess_n$ \dotfill \pageref{symbol:Q} \\
%   $z$ & \ The variable of the minimum problem we simplified \dotfill \pageref{symbol:z} \\
% \end{longtable}
% 
%  \newpage

\maketitle

\tableofcontents
 \section{Problem Statement}
 \begin{framed}
 (P) is a sparse linear system of the form:
 \[
 \begin{bmatrix} \label{eq:1}
D & E^T\\
E & 0
\end{bmatrix}
\begin{bmatrix}
x\\y
\end{bmatrix}
=
\begin{bmatrix}
b\\c
\end{bmatrix}
 \]
 where $D\in\mathbb{R}^{m\times m}$ is a diagonal positive definite matrix (i.e., $D = \operatorname{diag}( D ) > 0$
) and $E\in\mathbb{R}^{(n-1)\times m}$ is obtained by removing the last row from the node-arc incidence matrix of a given connected directed graph. These problems arise as the KKT system of the convex quadratic separable Min-Cost Flow Problem, hence you can look, e.g., \href{https://commalab.di.unipi.it/datasets/mcf}{here} for ways to generate meaningful instances of the problem.\medskip
\begin{itemize}
 \item 
(A1) is GMRES, and you must solve the internal problems $\min \bigl\| H_n y - \|b\| e_1 \bigr\|$
 by updating the QR factorization of $H_n$
 at each step: given the QR factorization of $H_{n-1}$
 computed at the previous step, apply one more orthogonal transformation to compute that of $H_n$.
\item (A2) is the same GMRES, but using the following preconditioner
$$P = \begin{bmatrix}
     D^{-\frac12} & 0\\
     0 & R^{-T}
    \end{bmatrix}$$
    where $S$
 is either $S=ED^{-1}E^T$
 or a sparse approximation of it (to obtain it, for instance, replace the smallest off-diagonal entries of $S$
 with zeros), and $S = R^TR$ is its Cholesky factorization.

\end{itemize}
No off-the-shelf solvers allowed.

 \end{framed}
\newpage
 \section{GMRES}
 
GMRES is an iterative method for computing an approximation of the solution to the linear system $Ax = y$. It accomplishes such approximation by computing:
 \[\min_{x \in K_n(A, y)} \|Ax - y\|\]
 i.e. by finding the minimum $x$ within the \emph{Krylov Space} $K_n(A, y)$ such that the norm of the residual $Ax - y$ is minimized.
 \subsection{Background: Krylov Spaces and Arnoldi} 
 Given a non-necessarily-symmetric matrix (although in our case it is) $A \in \rmatr mm$, a vector $y \in \rvect m$, and a natural number $n\leq m$:
 \[K_n(A, y) = \spann (y, Ay, A^2y, \hdots, A^{n-1}y).\]
 We can obtain a good basis for this vector space by using the Arnoldi Algorithm:
 \[\arnoldi(A, y, n) =: Q_n, \hess_n\]
 Where:
 \begin{itemize}
  \item $Q_n \in \rmatr nm$ is the matrix $(q_1 \mid \hdots \mid q_n)$ of vectors in the basis;
  \item $\hess_n \in \rmatr {n+1} {n}$ is a matrix such that:
  \begin{enumerate}
   \item 
  \[\underbar H_n = \begin{bmatrix}
                     H_n\\
                     ...
                    \end{bmatrix}
\]
where $H_n \in \rmatr nn$ is s.t. $H_n = Q_n ^T A Q_n$, and can therefore be seen as $A$ in the Krylov subspace.
\item
  \begin{equation}
AQ_n = Q_{n+1} \hess_n \label{arnoldi_eq}
  \end{equation}
  \end{enumerate}

 \end{itemize}
 
 \begin{remark} \label{q1} The first vector of the basis generated by $\arnoldi(A, y, n)$ is $\dfrac y {\|y\|}$.
 \end{remark}

 \subsection{Simplifying the minimum problem}
 We can simplify the minimum problem to: $$\min_{z \in \reals^{n+1}} \|\hess_n z - e_1\|y\|\|$$
 \begin{proof}
 \begin{align}
  \min_{x \in K_n(A, y)} \|A x - y\| &= \min_{z \in \reals^{n+1}} \|A Q_n z - y\| \tag{$q_i$s form basis of $K_n(A, y)$}\\
  &= \min_{z \in \reals^{n+1}} \|Q_{n+1}\hess_n z - y\| \tag{by \eqref {arnoldi_eq}}\\
  &= \min_{z \in \reals^{n+1}} \|Q_{n+1}^T(Q_{n+1}\hess_n z - y)\| \tag{$Q_{n+1}^T$ orthogonal}\\
  &= \min_{z \in \reals^{n+1}} \|\hess_n z - Q_{n+1}^T y)\| \tag{$Q_{n+1}$ orthogonal}\\
  &= \min_{z \in \reals^{n+1}} \|\hess_n z - Q_{n+1}^T q_1 \|y\|\| \tag{by Remark \ref{q1}}\\
  &= \min_{z \in \reals^{n+1}} \|\hess_n z - e_1\|y\|\| \tag{$Q_{n+1}^T$ orthogonal}
 \end{align}

 \end{proof}

 \newpage


 \subsection{Lanczos}
 
 Since the input matrix $A$ is symmetric, we can use the Lanczos Algorithm to iteratively construct the orthonormal basis for the Krylov space. In particular, the $H_n$ matrix resulting from such algorithm will by symmetric and tridiagonal:
 \[H_n = \begin{bmatrix}
    \alpha_1 & \beta_1\\
    \beta_1 & \alpha_2 & \beta_2\\
    &\beta_2 & \alpha_3 & \ddots\\
    &&\ddots & \ddots & \beta_{m-2}\\
    &&&\beta_{m-2} &\alpha_{m-1}& \beta_{m-1}\\
    &&&& \beta_{m-1} & \alpha_m
   \end{bmatrix}
\]
This allows us to perform the computations without storing the whole matrix, but only the diagonal and subdiagonal. The algorithm is as follows:
\begin{algorithm} \caption{Lanczos Algorithm}
\begin{lstlisting}
function Lanczos(A, y, n)
    
    $\alpha$ = $\beta$ := $\mathbf 0 \in \reals^n$      # diagonal and subdiagonal of H
    $L$ := $\mathbf 0 \in \reals^{m \times n}$        # Lanczos Basis
    
    # base case
    $q$ := $\frac{y}{\|y\|}$
    $L$[:, 1] := $q$
    $w$ := $A * q$
    $\alpha$[1] := $w^T$ * $q$
    $w$ := $w$ - $\alpha$[1] * $q$
    $\beta$[1] := $\|w\|$

    # inductive case
    for $j$ in 2...$n$
        if $\beta$[j-1] < 1e-13
            println("Breakdown");
            return
        end
        $q$ = $\frac w {\beta[j-1]}$
        $L$[:, $j$] = $q$
        
        $w$ := $A * q$
        $\alpha$[j] := $w^T$ * $q$
        $w$ := $w$ - $\alpha[j] * q - \beta$[$j$-1] * L[:, $j$-1]
        $\beta$[j] = $\|w\|$
        
    end 
    
    return (L, $\alpha$, $\beta$)

end
\end{lstlisting}
\end{algorithm}

\newpage
\subsection{Lanczos + QR}
A trivial way to implement GMRES would be:
\begin{itemize}
 \item Use Lanczos to obtain a basis for the Krylov space
 \item Compute the QR factorization of $\hess_n$
 \item Use the factorization to easily solve the linear system arising from the minimum problem.
\end{itemize}
However, the requested version should compute the QR factorization during the Lanczos iteration.

\subsubsection{Extending the QR decomposition}
Assume we have $\underbar H_{n-1} = Q_{old}R_{old}$. The matrix $\underbar H_n$ will be computed using a Lanczos iteration, obtaining:
\[\underbar H_n = \left[\begin{array}{ c | c }
    \underbar H_{n-1} & \begin{matrix}
           0\\
           \vdots\\
           0\\
           \beta_{n-1}\\
           \alpha_n\\
          \end{matrix}
 \\
    \hline
    0 \quad\hdots\quad 0 & \beta_n
  \end{array}\right]
\]
We want to find two matrices $Q$, $R$ such that:
\begin{itemize}
 \item $Q\in \rmatr{(n+1)}{(n+1)}$ is orthogonal
 \item $R \in \rmatr{(n+1)}{n}$ is upper triangular
 \item $\underbar H_n = QR$, i.e. $Q^T \underbar H_n = R$
\end{itemize}
First off, to obtain a $Q$ matrix of the requested shape that is orthogonal, we could consider:
\[Q_{old}' = \left[\begin{array}{ c | c }
    Q_{old} & \begin{matrix}
           0\\
           \vdots\\
           0
          \end{matrix}
 \\
    \hline
    0 \quad\hdots\quad 0 & 1
  \end{array}\right]
\]
This takes us close to the solution, but not quite there; in fact the product $Q'^T_{old} \underbar H_n$ is \emph{almost} triangular:
\[Q'^T_{old} \underbar H_n = \left[\begin{array}{ c | c }
    R_{old} & 
           Q'^T_{old}\begin{bmatrix}
            0\\
            \vdots\\
            0\\
            \beta_{n-1}\\
            \alpha_n
           \end{bmatrix}
 \\
    \hline
    0 \quad\hdots\quad 0 & \beta_n
  \end{array}\right]
\]
Recall that $R_{old}$ is an $n \times (n-1)$ upper triangular matrix, hence it has a row of zeros at the bottom. To make this matrix triangular, we should have a zero in place of the $\beta_n$. We can get rid of such zero by performing a Householder reflection on the bottom two elements of the final column. Let these be:
\[x = \begin{bmatrix}
       a \\ \beta_n
      \end{bmatrix}
\quad\quad \mapsto \quad\quad y = \begin{bmatrix}
       \|x\| \\ 0
      \end{bmatrix}\] 
  As seen in class, the transformation we need is a reflection w.r.t $v = x - y$:
  \[H_{refl} = I - \dfrac{2 vv^T}{\|v\|^2}\]
  In order for the reflection to only act on the two bottom elements of the final row, we extend it as follows, obtaining another orthogonal transformation:
  \[O = \left[\begin{array}{ c | c }
    I & \begin{matrix}
         0&0\\
         \vdots &\vdots \\
         0&0
        \end{matrix}

 \\
    \hline
    \begin{matrix}
         0 \hdots 0\\0 \hdots 0
        \end{matrix} & H_{refl}
  \end{array}\right]\]
  The matrix we obtain as $OQ_{old}'^T\underbar H_n$ is triangular.
  
  \paragraph{Constructing the $R$ Matrix}
  \begin{itemize}
   \item Get old $R$;
   \item Compute:
   \[Q'^T_{old}[
            0,
            \hdots,
            0,
            \beta_{n-1},
            \alpha_n]^T= ([
            0,
            \hdots,
            0,
            \beta_{n-1},
            \alpha_n]Q'_{old})^T\]
  Let $q_1 ... q_n$ be the columns of $Q'_{old}$. The result is:
  \[\sum_i \beta_{n-1} q_{i, n-1} + \alpha_{n} q_{i, n}\]
  This can be rewritten, letting $r_1...r_n$ be the rows of $Q'_{old}$, as:
  \[\beta_{n-1}r_{n-1} + \alpha_n r_{n}\]
  \item Perform the $O$ transformation to obtain $R$.
  \end{itemize}

  \paragraph{Constructing the $Q$ matrix}
  \begin{itemize}
   \item Get old $Q$;
   \item Construct $Q_{old}'$ by adding the extra row and column;
   \item Compute $Q = (O Q'^T_{old})^T$. 
   
  \end{itemize}
  
  Altenatively we can calculate the new Q as follows:
  \[Q = O^T Q'_{old} = OQ_{old}\]
  By the symmetry of $O$, which is guaranteed by the symmetry of $H_{refl}$. Computationally, we only need to update the last two columns of $Q$, so we need not construct $O$ explicitly. The following algorithm illustrates our approach:

  \begin{algorithm}
\caption{Construction of $Q$ matrix}
\begin{lstlisting}
  $Q[j+1, j+1]$ := 1.0
  newcols := $\mathbf{0} \in \reals^{(j+1) \times 2}$
  for $i$ in 1...$j$+1
    newcols[$i$, 1] := $Q$[i, j] * $H_{refl}$[1,1] + $Q$[i, j+1] * $H_{refl}$[2,1]
    newcols[$i$, 2] := $Q$[i, j] * $H_{refl}$[1,2] + $Q$[i, j+1] * $H_{refl}$[2,2]
  end
  Q[$1$:$j$+1, $j$:$j$+1] := newcols
\end{lstlisting}

  \end{algorithm}

  
  \newpage
  \subsection{Solving the minimum problem, given QR}
  Given the QR factorization of $\underbar H_n$, we can transform our minimum problem:
  \[\boxed{\min_{z \in \reals^{n+1}} \|\hess_n z - e_1\|y\|\|}
  \text{ \quad into \quad }\boxed{\min_{z \in \reals^{n+1}} \|R_0 z - Q_0^T (e_1 \|y\|)\|}.\]
  \begin{proof} Let $\underbar H_n = QR$, \quad $Q = [Q_0, Q_c]$, \quad $R = [R_0, 0]^T$.
  
  \begingroup
\addtolength{\jot}{.5em}
   \begin{align}
     \|\hess_n z - e_1\|y\|\| &= \|Q^T (\hess_n z - e_1\|y\|)\| \tag{$Q^T$ is norm-preserving}\\
     &= \|Q^TQRz - Q^T( e_1\|y\|)\| \tag{QR factorization of $\underbar H_n$}\\
     &= \left \| \begin{bmatrix}R_0 \\ 0\end{bmatrix} z - \begin{bmatrix}Q_0^T \\ Q_c^T\end{bmatrix} ( e_1\|y\|) \right \| \tag{assumption}\\
     &= \left \|\begin{bmatrix}
         R_0 z - Q_0^T ( e_1\|y\|)\\
         -Q_c^T (e_1\|y\|)
        \end{bmatrix}\right \| \tag{vector algebra}
   \end{align}
   Whose minimum is only influenced by the top portion (the bottom one does not depend on $z$).
\endgroup
  \end{proof}
\noindent Once we have transformed the problem to this form, if $R_0$ is invertible (which is true iff the $\underbar H_n$ is full-rank), we have obtained a \emph{triangular system}, which is efficiently solved by backsubstitution.

\subsection{Computational Complexity}

\subsubsection{Time Complexity of Lanczos+QR}
The algorithm consists of a for-loop of length $n$ (the number of Lanczos iterations). We go through the dominating parts of the $j$-th step of the loop:
\begin{itemize}
 \item The Lanczos iteration's complexity, both in its base and inductive cases, is dominated by the matrix-vector multiplication $w := Aq$, which has time complexity $O(m^2)$ ($m$ is the size of $A$).
 \item The base case of the QR construction consists of a $2\times 2$ QR factorization, which can easily be considered as a constant-time operation.
 \item The inductive case of the QR construction is dominated by the $O(j)$ cost of computing the new columns of the $Q$ and $R$ matrices.
\end{itemize}
This yields a time complexity of
\[O \left(nm + \dfrac{n(n+1)}{2} \right) = O(nm + n^2)\]
Taking in account the fact that we use a sparse matrix data type in the implementation, we can express the time complexity as follows:
\[O(\texttt{nnz(A)} + n^2)\]
\subsubsection{Space Complexity of Lanczos+QR}
Let $\#D$ represent the length of $D$'s diagonal. We need to store:
\begin{itemize}
 \item The matrix (in sparse form): $O(\#D + 2\cdot \texttt{NNZ(E)})$
 \item The Lanczos basis: $O(m \cdot n)$
 \item $Q$ and $R$: $O(n^2)$
\end{itemize}
Hence the space complexity is the sum of the above complexities.

\subsubsection{Time complexity of GMRES}

Our implementation of GMRES consists of:
\begin{itemize}
 \item A Lanczos+QR computation $O(\texttt{nnz(A)} + n^2)$
 \item Solving an $n\times n$ triangular system: $O(n^2)$
\end{itemize}
Hence the complexity is dominated by the Lanczos+QR computation $O(\texttt{nnz(A)} + n^2)$.

\subsubsection{Space complexity of GMRES}
Once again, the space complexity is dominated by that of Lanczos+QR, it is therefore:
\[O(\#D + 2\cdot \texttt{NNZ(E)} + mn + n^2)\]

\subsection{Stability}

The three components of our method are the Lanczos method, the QR factorization and the back substitution used to solve the triangular linear system resulting from the QR factorization.

\begin{itemize}
 \item \textbf{Lanczos} \quad As remarked in \cite{trefethen2022numerical}, \emph{``no straightforward theorem is known to the effect that the Lanczos iteration is stable in the sense defined in this book''}.
 \item \textbf{QR} \quad As for the QR factorization, we provide a proof of its backward stability.
 \item \textbf{Back substitution}\quad A proof of the backward stability of back substitution can be found in \cite{higham2002accuracy}.
\end{itemize}


\subsubsection{Background: Backward Stability}

An algorithm is said to be \emph{backward stable} if the computed output $\widetilde y$ can be written as $\widetilde y = f(\widehat x)$, where:
\[ \dfrac{\|\widehat x - x\|}{\|x\|} = O(u) \]
where $\widehat x$ is a perturbed input $x + \Delta x$.

\subsubsection{Backward Stability of QR factorization}

First, we recall the following result:
\begin{theo}[Backward stability of orthogonal transformations] \label{orthobs}
If $Q \in \mathbb R^{m \times m}$ is orthogonal, and $B \in \mathbb R^{m \times n}$, then the computed version $\widetilde C$ of $C = QB$ is backward stable:
\[ \widetilde C = Q\widehat B, \quad\quad \|\widehat B - B\| \leq O(u) \|B\| \]
\end{theo}
\noindent The proof is omitted. We can now prove the following:
\begin{framed}
\begin{claim}
Let $A + \Delta_A$ be a perturbed version of the input matrix $A$. We compute:
\[ \widetilde Q \widetilde R = \texttt{QR}(A + \Delta_A). \]
The computed $\widetilde Q$ and $\widetilde R$ satisfy:
 \[ \widetilde Q \widetilde R = A + \Delta \quad \text{ with  } \quad \|\Delta\| \leq O(u)\|A\|.\]
\end{claim}
\end{framed}

\begin{proof}
 Recall that:
 \[ A_0 = A,\quad\quad A_{k} = Q_k\hdots Q_1 A,
 \quad\quad k \in \{1..n\}\]
Where the $Q_k$s are orthogonal matrices of the form:
\[\begin{bmatrix}
   I & 0\\
   0 & H_k
  \end{bmatrix}
\]
And $H_k$ is a Householder reflector matrix. We first show by induction that:
\[ \widetilde A_k := \widetilde Q_k (\widetilde A_{k-1} + \Delta) \]
is such that $\|\Delta\| \leq O(u)\|A\|$ and $\|\widetilde A_k\| \leq \|A\| + O(u) \|A\|$.
\begin{itemize}
 \item \textbf{Base case}\quad By Theorem \ref{orthobs}:
 \[ \widetilde A_1 = \widetilde Q_1(A + \Delta_1), \quad\quad \|\Delta_1\| \leq O(u) \|A\|\]
 Additionally:
 \begin{align*}
  \|\widetilde A_1\| &= \|\widetilde Q_1 (A + \Delta_1)\|\\
  &=\|A + \Delta_1\| \tag{orthogonality}\\
  &\leq \|A\| + O(u) \|A\| \tag{prop. of norms, above ineq.}
 \end{align*}

 \item \textbf{Inductive Case} \quad Let us assume that $\widetilde A_{i} = \widetilde Q_i (\widetilde A_{i-1} + \Delta_i)$, with:
 \[ \text{(Hp 1)} \quad\|\Delta_i\| \leq O(u)\|A\| \quad\quad \text{(Hp 2)} \quad \|\tilde A_i\| \leq \|A\| + O(u) \|A\|.\]
 Then:
 \[ \widetilde A_{i+1} = \widetilde Q_{i + 1} (\widetilde A_i  + \Delta_{i+1}) \]
We thus have:
\begin{align*}
  \|\Delta_{i+1}\| &\leq O(u) \|(\widetilde A_i + \Delta_i)\|\tag{by Theorem \ref{orthobs}}\\
  &\leq O(u) (\|\widetilde A_i\| + \|\Delta_i\|) \tag{prop. of norms}\\
  &= O(u) (\|\widetilde A_i\| + O(u)\|A\|) \tag{Inductive Hp 1}\\
  &\leq O(u) (\|A\| + O(u) \|A\| + O(u) \|A\|) \tag{Inductive Hp 2}\\
  &= \|A\|O(u)
\end{align*}
And finally:
\begin{align*}
 \|\widetilde A_{i+1}\| &= \|\widetilde Q_{i + 1} (\widetilde A_i  + \Delta_{i+1})\|\\
&=   \|\widetilde A_i  + \Delta_{i+1}\| \tag{Orthogonality}\\
  &\leq \|\widetilde A_{i}\| + \|\Delta_{i+1}\| \tag{prop. of norms}\\
  &\leq \|A\| + O(u)\|A\| + \|\Delta_{i+1}\| \tag{Inductive Hp 2}\\
  &\leq \|A\| + O(u)\|A\| + \|A\|O(u) \tag{above inequality}\\
  & = \|A\| + O(u) \|A\|
\end{align*}

\end{itemize}
Now recall that $\widetilde R = \widetilde A_k$ for some $k$, and therefore, given:
\[ \widetilde Q \widetilde R = A + \Delta \]
we can conclude:
\begin{align*}
 \|\Delta\| &\leq O(u)\|A_k\| \tag{by Theorem \ref{orthobs} }\\
 &\leq O(u) (\|A\| + O(u)\|A\|) \tag{as shown by induction}\\
 &= O(u)\|A\|
\end{align*}

\end{proof}

\newpage
\section{GMRES with preconditioner}

In this section we discuss how we implemented the algorithm using the following preconditioner: $$P = \begin{bmatrix}
     D^{-\frac12} & 0\\
     0 & R^{-T}
    \end{bmatrix}$$
    where where $S$
 is either $S=ED^{-1}E^T$
 or a sparse approximation of it, and $S = R^TR$ is its Cholesky factorization.
 Since our matrix is symmetric, the preconditioned problem we want to solve is:
    \[ PAP^T(P^{-T}x) = Py \]

    \subsection{Implementation details}
    We do not construct the preconditioner explicitly. Rather, we perform multiplications by $PAP^T$ as:
        \lstset{style=mystyle_tt}
    \begin{lstlisting}
w = mult_by_PT(q, sqrtD, Uinv)
w = A * w
w = mult_by_P(w, sqrtD, Uinv)\end{lstlisting}
    Where \texttt{sqrtD} is $D^{-1/2}$, \texttt{Uinv} is $U^{-1}$, and:
\begin{lstlisting}
function mult_by_P(v, sqrtD, Uinv)
    sizeD = size(sqrtD)[1]
    sizeP = sizeD + size(Uinv)[1]

    bu = v[1 : sizeD]
    bb = v[sizeD + 1 : sizeP]
    v = [sqrtD * bu ; Uinv' * bb]

    return v
end

function mult_by_PT(v, sqrtD, Uinv)
    return mult_by_P(v, sqrtD, Uinv')
end\end{lstlisting}



\paragraph{Approximation of $S$} Once $S$ is computed, we can remove all off-diagonal elements under some threshold using the following Julia code:
\begin{lstlisting}
  # set off-diagonal elements under some threshold to zero
  if THRESHOLD_ZERO > 0
      threshold = THRESHOLD_ZERO
      mask = .!I(heightE) .& (abs.(S) .< threshold)
      S[mask] .= 0
  end
\end{lstlisting}
\subsubsection{Complexity and stability}

The results for the computational complexity and stability of the algorithm remain unchanged from those stated previously, for the algorithm without preconditioning. The one difference that had an impact on performance was the increased density of the preconditioned matrices, which makes it so that the preconditioned version of the algorithm takes longer to compute.

\newpage

\section{Additional implementations}

\subsection{Restarted versions of the algorithms}

In addition to the GMRES implementation, we provided a \emph{higher-order function} that takes as input any GMRES implementation, and uses it to perform the \textbf{restarted GMRES} algorithm, proposed as an exercise in chapter 35 of \cite{trefethen2022numerical} and further discussed in \cite{saad2003iterative}.

\paragraph{Reasons for using the restarted version}

For larger values of $n$, the cost of GMRES in operations and storage may be prohibitive. This is because, as seen before, the algorithm is quadratic both in time and in space. One way to mitigate these issues is to \emph{restart} the algorithm after a certain number of iterations. The restarted GMRES algorithm, as implemented in Julia, is as follows:

\begin{lstlisting}[mathescape = False]
function GMRES_Restarted(GMRES_Method :: Function, A::SparseMatrixCSC, y::Vector, args :: Tuple, max_iter::Int, restart::Int, tol::Float64)
    x = spzeros(size(A, 2))

    # Current residual
    r = y - A * x
    norm_r = norm(r)

    iter = 0
    while norm_r > tol && iter < max_iter

        # Perform GMRES for 'restart' iterations
        dx = GMRES_Method(args..., Vector(r), restart)

        # Update solution and residual
        x += dx
        r = y - A * x
        norm_r = norm(r)

        iter += restart

        println("Iteration $iter, Residual Norm: $norm_r")
    end

    if norm_r <= tol
        println("Converged in $iter iterations with residual norm $norm_r")
    else
        println("Did not converge within $max_iter iterations. Final residual norm: $norm_r")
    end

    return x
end
\end{lstlisting}


\paragraph{Correctness: restart invariant}
 Let $r_i$ be the residual after restart $i$: $r_i = y-Ax_{i}$. We have the following:
$$Ax_i = y - r_i$$
  $$x' := \texttt{GMRES}(A,r_i) \implies Ax' = \widetilde r_i \approx r_i$$
  Let $x_{i+1} = x_i + x'$, now:
  \[A(x_{i+1}) = Ax_i + Ax' = y - \widetilde r_i + r_i \approx y\]
This shows that, at each restart:
\[A x_i \approx y.\]
Indeed, we should ensure that, under some assumptions, the approximations grow in precision with each iteration, but we do not go into this: with the right parameter choice, we found experimentally that the method provides very good approximations of the solution in very brief time.

\paragraph{Drawbacks of the method}

A well known difficulty with the restarted GMRES algorithm is that it can stagnate when the matrix is not positive definite \cite{saad2003iterative}. Indeed, experimentally, we have noticed that choosing the wrong parameters (\texttt{restart} and tolerance) would yield stagnation. However, as evident from the experimental data, the restarted algorithm often delivered solutions orders of magnitude more fast and precise than the bare implementations.

\paragraph{Computational cost of restarted}

We have to notice that, since we lose some optimality condition on the method by using the restart we have to do a tradeoff
between how much the Krylov space we work making each \textit{iterate} slower and the number of iteration that will be required to converge, 
for example by setting the iteration to a very low value we have a very fast \textit{iterate} due to the small Krylov space but we may lose the 
convergence of the method or need a lot of \textit{iterates} to converge. So coupling this by carefully choosing the restart parameter along a good preconditioner
we can obtain a very fast and precise solution.


\begin{itemize}
    \item \textbf{Time complexity} Given the Lanczos+QR implementation, 
    the time complexity of the restarted GMRES is $O(\texttt{nnz(A)} + n_r^2$), where $n_r$ is the number of iterations chosen for restarts.
    \item \textbf{Space complexity} The space complexity is the same as the GMRES algorithm.
\end{itemize}

\section{Generating instances of the problem}

\subsection{Min-Cost Flow}

The problem arises from the KKT system of the convex quadratic separable Min-Cost Flow Problem. The problem is defined as follows:
\begin{itemize}
 \item Let $G = (V, A)$ be a graph, with $V$ its set of nodes, and $A$ its set of arcs.
 \item Let $b \in \mathbb R ^{|V|}$ be a vector of \emph{node flows} (based on which we can classify nodes as source, target or transshipment).
 \item Let $x \in \mathbb R^{|A|}$ be a \emph{flow assignment}:
 \[x = \begin{bmatrix}
    \varphi_1\\
    \vdots\\
    \varphi_{|A|}
   \end{bmatrix}
\]
\item Let $\varphi_{ij}$ be the flow on the edge $(i, j) \in A$.
\item Let $C_{ij} : \reals \to \reals$ be a quadratic function that maps a flow $\varphi$ to the \emph{cost} of transporting such flow through the $(i, j)$ edge.
\end{itemize}

We formulate the problem as follows:
\[ \min \sum_{(i, j) \in E} C_{ij} (\varphi_{ij}) \]
With the constraint given by Kirchhoff's law of conservation of flow:
\[ \forall i \in V . \sum_{\scriptsize \makecell{i \in V\\(j, i) \in A}} \varphi_{i, j} - \sum_{\scriptsize \makecell{i \in V\\(i, j) \in A}} \varphi_{i, j} = b_i.\]
There would be other constraints, e.g. the non-negativity of flows or per-edge upper and lower bounds to the flows, but in order to obtain a KKT system of the requested shape we do not consider these.

\subsection{Problem and constraints in matrix form}
We mentioned that each function $C_{ij}(\varphi)$ is quadratic. Let us write it as:
\[ \forall e \in A \;.\; C_{e} (\varphi) = \dfrac12 c_{e} \varphi^2 + d_{e} \varphi \]
We can thus rewrite the minimum problem as:
\[\min \left ( x^T D x + \langle d, x \rangle \right )
\]
Where:
\[D = \begin{bmatrix}
            c_1 &  & 0\\
             &\ddots & \\
            0  & & c_{|A|}\\
           \end{bmatrix}\quad\quad d =
           \begin{bmatrix}
           d_1\\
           \vdots\\
           d_{|A|}
           \end{bmatrix}
           \]
Additionally, given the incidence matrix $\underline E$, we can write the constraint as simply $\underline E x = b$.
\subsection{KKT System}
From the above problem and equality constraint, we obtain the following system:
\begin{align}
 \underline{E} x &= b \tag{KKT-F}\\
 Dx + \underline{E}^T \cdot \mu &= -d \tag{KKT-G}
\end{align}
With $\mu \in \reals$. The system can be rewritten as:
\[
\begin{bmatrix}
D & \underline E^T\\
\underline E & 0\\
\end{bmatrix}\begin{bmatrix}
x\\ \mu
\end{bmatrix} = \begin{bmatrix}
-d\\b
\end{bmatrix}
\]
In order to obtain an equivalent full-rank system, as suggested in the problem statement, we can remove one row from the incidence matrix.

\subsection{Netgen dataset, and generating $D$}
In order to obtain realistic instances to test our algorithms, we used the netgen \cite{netgenwebsite} dataset. This provides graphs in the DIMACS format defined in \cite{dimacsminflow}, coming with the following information:
\begin{itemize}
 \item Node flow of each vertex
 \item Source, target, capacity, and \emph{linear} cost of each edge.
\end{itemize}
The main issue with this dataset is that, indeed, it only provides linear costs.

\paragraph{Generating $D$} Frangioni et al. \cite{frangioni2011projected} propose a procedure for generating realistic quadratic costs. Their approach is to generate the costs uniformly in $[ac_{ij}, bc_{ij}]$, where $c_{ij}$ are the linear costs and $a,b$ are integer numbers. The interval considered in the paper was $[3c_{ij}, 10c_{ij}]$.

\paragraph{Other ways to construct $D$} In order to further explore the performance of our algorithm, we will additionally test with other versions of the matrix $D$, namely:
\begin{itemize}
 \item Diagonal with all ones
 \item Diagonal with entries uniformly distributed in $[0,1]$
\end{itemize}
\newpage
\section{Experimental Results}

The following experimental results refer to a 9215$\times$9215 matrix, and were ran on a laptop with a \emph{13th Gen Intel(R) Core(TM) i5-1335U} processor, and 16 GB of RAM.

\subsection{Comparison in performance for different $D$}

 As outlined in Figure \ref{fig:Dcompare}, we obtained the following results:
 \begin{itemize}
  \item With $D = I$, the algorithm reaches convergence to $~10^{-10}$ in fewer than 200 steps
  \item With the uniform distribution, the algorithm settles on $~10^{-8}$ in almost 1500 steps.
  \item The matrix with the generated quadratic costs is quite ill-conditioned, with a condition number of $1.347 \cdot 10^{11}$. Therefore, the algorithm does not quite converge at all.
 \end{itemize}


\begin{figure}[H]
 \begin{minipage}{.45\textwidth}
 \begin{center}
  \includegraphics[scale = .18]{./plots/all ones}
 \end{center}

 \end{minipage}
  \begin{minipage}{.45\textwidth}
 \begin{center}
  \includegraphics[scale = .18]{./plots/uniform}
 \end{center}
 \end{minipage}


 \begin{center}
  \includegraphics[scale = .18]{./plots/generated}
 \end{center}
 \caption{Residuals vs iterations for different constructions of $D$.}
\label{fig:Dcompare}
\end{figure}

\subsection{Comparison between the precond. and non-precond. systems}

In this section we compare the preconditioned algorithm to the non-preconditioned one. In Figure \ref{fig:precond}, we show how:
\begin{itemize}
 \item For the matrix with $D = I$, we remark that the preconditioned algorithm converges much faster, albeit to a slightly less precise result.
 \item The same is true for the matrix with elements in $[0,1]$.
 \item The preconditioned algorithm performs the best with the ill-conditioned matrix obtained generating the quadratic weights. Not only does it converge fast, but the result is several orders of magnitude more precise than the non-preconditioned algorithm.
\end{itemize}

From these observations, in particular the one regarding the ill-conditioned matrix, we conclude that the preconditioner works. The reason why the performance is not improved in the first two cases, is that the two matrices were already well-conditioned, as shown in the following table.

\begin{center}
{ \renewcommand{\arraystretch}{1.5}
\begin{tabular}{|l|c|c|}
\hline
& \textbf{Non-precond.} & \textbf {Precond.}\\
\hline
\textbf{All ones} & $7.104 \cdot 10^{3}$  & 2.618 \\
\textbf{Uniform in $[0,1]$} & $2.74 \cdot 10^{3}$ & 2.618 \\
\textbf{Generated Quadratic} & $1.347 \cdot 10^{11}$ &2.618 \\
\hline
\end{tabular}
}
\end{center}
The value of the condition number for all three preconditioned matrices, $\sim$2.61803398875, appears to be the \emph{golden ratio} ($\varphi$) plus one.

\begin{figure}
  \begin{minipage}{.45\textwidth}
 \begin{center}
\includegraphics[scale = .18]{./plots/precond all ones}
 \end{center}

 \end{minipage}
  \begin{minipage}{.45\textwidth}
 \begin{center}
\includegraphics[scale = .18]{./plots/precond uniform}
 \end{center}

 \end{minipage}

  \begin{center}
\includegraphics[scale = .18]{./plots/precond generated}
 \end{center}

 \caption{Comparison between preconditioned and non-preconditioned system.}
 \label{fig:precond}
\end{figure}

\newpage
\subsection{Comparison between GMRES and Restarted GMRES}

As mentioned, for the correct choice of the \texttt{restart} parameter, the restarted version of the algorithm is able to provide faster and more precise results. In figures \ref{fig:restarted_ones},\ref{fig:restarted_uniform},\ref{fig:restarted_generated} we compare all the methods described in this report, including the restarted ones. Once again, we used a 9215$\times$9215 matrix from the \texttt{netgen} dataset, on the same setup as above.

\subsubsection{Matrix with $D = I$}

With this particularly well-behaved matrix, all proposed methods converge very fast, the fastest one being the restarted method (with \texttt{restart} = 300), and the most precise being the restarted preconditioned method, which reaches $10^{-10}$. The convergence times are all well below one second.

\begin{figure}[H]
   \begin{center}
\includegraphics[scale = .2]{./plots/all all ones}
 \end{center}
 \caption{Comparison of all methods for $D=I$.}
 \label{fig:restarted_ones}
\end{figure}

\subsubsection{Matrix with diagonal in [0,1]}
With this matrix, the fastest converging method is the preconditioned algorithm, which reaches convergence in less than one second. The most precise algorithm is still the restarted preconditioned algorithm (with \texttt{restart} = 300), which reaches a precision of $10^{-10}$ in less than two seconds.

\begin{figure}[H]
 \begin{center}
\includegraphics[scale = .2]{./plots/all uniform}
 \end{center}
 \caption{Comparison of all methods for $D$ uniform in $[0,1]$.}
 \label{fig:restarted_uniform}
\end{figure}

\subsubsection{Matrix with generated quadratic costs}
With this ill-conditioned matrix, GMRES and Restarted GMRES do not reach convergence. The preconditioned algorithm converges to $10^{-7}$ in less then one second, whereas the restarted preconditioned (with \texttt{restart} = 300) algorithm reaches $10^{-10}$ in less than two seconds.

 \begin{figure}[H]
  \begin{center}
  \includegraphics[scale = .2]{./plots/all generated}
 \end{center}

 \caption{Comparison of all methods for generated quadratic costs.}
 \label{fig:restarted_generated}
\end{figure}

\newpage

\section{Command Line Tool}

We wrote a command line tool to facilitate the testing of our algorithms. The following is its usage, which can be obtained by writing \texttt{julia -p 1 main.jl --help}

\lstset{style=mystyle_plain}
\begin{lstlisting}
usage: main.jl [--iterations ITERATIONS] [--restart RESTART]
               [--restart_precond RESTART_PRECOND]
               [--diagonal DIAGONAL] [--tol TOL] [--step STEP]
               [--precond] [--restarted] [--nworkers NWORKERS] [-h]
               mode data

positional arguments:
  mode                  Program mode. Can be "compute" or "bench".
  data                  Path to the problem data.

optional arguments:
  --iterations ITERATIONS
                        Number of iterations. (type: Int64, default:
                        1000)
  --restart RESTART     Restart parameter of restarted algorithm.
                        (type: Int64, default: 300)
  --restart_precond RESTART_PRECOND
                        Restart parameter of restarted preconditioned
                        algorithm. (type: Int64, default: 300)
  --diagonal DIAGONAL   Can be "ones", "uniform" or "quadratic"
                        (default: "quadratic")
  --tol TOL             Tolerance parameter of restarted algorithm.
                        (type: Float64, default: 1.0e-9)
  --step STEP           Step between iter values in bench mode. If 0,
                        uses iterations/4 (type: Int64, default: 0)
  --precond             Preconditioned? [used in compute mode].
  --restarted           Restarted? [used in compute mode].
  --nworkers NWORKERS   Number of workers for the conversion function.
                        Should be used in tandem with "julia -p
                        nworkers" (type: Int64, default: 1)
  -h, --help            show this help message and exit
\end{lstlisting}

\subsection{Data generation} We provide a parser (\texttt{parser\_matrix.py}) that transforms the \texttt{dmx} files from the netgen dataset to \texttt{npz} compressed archives. In the dataset directory, we include some compressed data representing graphs of increasing size and density.

\subsection{Conversion function}
In order to have the data occupy less space, we used the \texttt{npz} data format to store them. However, this is not directly compatible with the Julia sparse matrix format, therefore to load the incidence matrices from disk we first run them through a conversion function we wrote. \medskip

\noindent We provide two versions of the conversion function: a serial one and a parallel one.

\begin{itemize}
 \item For incidence matrices that are small enough, the serial converter completes the conversion faster than the parallel one. The conversion times are usually around 1 or 2 seconds for this kind of matrices.
 \item For larger matrices, such as the one obtained from: \texttt{net12\_64\_1.dmx\_out.npz}, the serial algorithm is way too slow. In these cases, it is necessary to use the parallel one.

\end{itemize}


\subsection{Examples}

In order to simplify the usage of the tool, we provide a \textbf{makefile}, with two targets:

\begin{itemize}
 \item \textbf{run}, which performs the serial conversion:

 \begin{lstlisting}
  make run ARGS="compute ./dataset/net10_8_1.dmx_out.npz --precond --iterations 200"
 \end{lstlisting}


 \item \textbf{run\_mp}, which stands for ``multi-process'', and performs the parallel conversion:
  \begin{lstlisting}
  make run_mp ARGS="compute ./dataset/net12_64_1.dmx_out.npz --precond --iterations 200" NWORKERS="10"
 \end{lstlisting}
\end{itemize}

\paragraph{Modes} The command line tool has two ``modes''. \textbf{Compute mode} can be used to perform the algorithm on a graph from the dataset.

 \begin{lstlisting}
$ make run ARGS="compute ./dataset/net10_8_1.dmx_out.npz --precond --iterations 200"
julia -p 1 main.jl compute ./dataset/net10_8_1.dmx_out.npz --precond --iterations 200
converting data format...
converted in 1.581830902 seconds.

Compute mode
============
Performing 200 iterations:
residual: 1.1924300300246332e-7 computed in 0.849061762s
 \end{lstlisting}
On the other hand, \textbf{bench mode} can be used to plot the comparison between the methods. In the following example, since the step is not specified through a command line option, it defaults to $\frac 14$ times the number of iterations.
 \begin{lstlisting}
$ make run ARGS="bench ./dataset/net10_8_1.dmx_out.npz --diagonal ones --iterations 200"
julia -p 1 main.jl bench ./dataset/net10_8_1.dmx_out.npz --diagonal ones --iterations 200
converting data format...
converted in 1.215956481 seconds.

Bench mode
==========
Performing 200 iterations with step = 50
Testing GMRES...
Testing precond GMRES...
 * [...]
Testing restarted GMRES...
 * [...]
Testing restarted precond GMRES...
 * [...]
press enter to terminate program.
 \end{lstlisting}
The program produces the scatter plot in figure \ref{fig:example}.
\begin{figure}
 \begin{center}
\includegraphics[scale = .5]{plots/example}
 \end{center}
\caption{Example plot.}
\label{fig:example}
\end{figure}

\subsection{Notes on iterations and memory usage}

Since in our algorithm it is necessary to store the $n$-dimensional basis of the Krylov space explicitly in memory, where $n$ is the number of iterations, a high number of iterations for a very large input matrix will result in high memory usage.

\subsection{Notes on the language}

\subsubsection{JIT and TTFX}
Julia is a dynamic programming language commonly used in numerical analysis. Its most important feature is \emph{just-in-time} (JIT) compilation. In our code, in order to get the timings correct, we had to make sure that each portion of code is compiled before it is timed. We did this by calling it with dummy values before performing the actual timing.\smallskip

\noindent One drawback of JIT compilation is the so-called ``time to first execution'' (TTFX). Each time we run a portion of a Julia script for the first time, there is a slight delay due to the fact that the code is being compiled.

\subsection{Dependencies} In Julia, packages are installed from whithin the language itself. The dependencies can be installed by running: \texttt{julia requirements.jl}, which will install the packages used in our project.

\section{Conclusions}

We implemented two algorithms, GMRES and preconditioned GMRES, and a higher-order function that provides the \emph{restarted} version of any GMRES implementation. The algorithm without preconditioning is not able to provide satisfying results with realistic, ill-conditioned systems. The preconditioned algorithm is, instead, adequate for the purpose. However, for the right choice of the \texttt{restart} parameter, the \emph{restarted preconditioned} algorithm is able to converge quickly to results much more precise than any of the other proposed algorithms, even in the case of ill-conditioned matrices.

\newpage

\printbibliography %Prints bibliography

\end{document}
